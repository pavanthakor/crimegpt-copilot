# Stub docker-compose — postgres only for now.
version: "3.9"

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: crimegpt
      POSTGRES_USER: crimegpt
      POSTGRES_PASSWORD: crimegpt
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
